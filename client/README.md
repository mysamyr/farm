Client (browser)

TypeScript client modules compiled and bundled to public/script.js.

Scripts:

- npm run client:build — builds once
- npm run client:dev — watch & rebuild (fast), use your server to serve public/
