export enum Env {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  COMPILE = 'compile',
  TEST = 'test',
}

export enum LogLevel {
  DEBUG = 'debug',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
}
